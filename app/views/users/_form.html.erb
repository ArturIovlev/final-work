<style>
.con{
    font-family:  "Lohit Tamil Classical";
    font-size: 15px;
}
.xet{
    font-family:  "Lohit Tamil Classical";
    border-radius: 10px;
    box-shadow: 0 0 15px 4px rgba(0,0,0,0.10);
}
.te{
    font-family:  "Lohit Tamil Classical";
    font-size: 27px;
}
form{
    font-family:  Arial, Helvetica, sans-serif;

    max-width: 250px;
    padding: 10px;
    border-radius: 10px;
    box-shadow: 0 0 15px 4px rgba(0,0,0,0.10);
}

</style>

<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= form.text_field :username,:class=>"form-control",placeholder: "Name" %>
  </div>

  <div class="field">
    <%= form.text_field :password,:class=>"form-control",placeholder: "Password" %>
  </div>

  <div class="actions">
    <%= form.submit :class=>"form-control" %>
  </div>

<% end %>